"use client";
import {useEffect,useMemo,useState} from"react";import{PageHeader}from"@/components/ui/PageHeader";import{subscribeResources}from"@/features/scheduler/firestore";import type{SchedulerResource}from"@/features/scheduler/types";import{createEmergency,subscribeEmergencies,updateEmergency}from"./firestore";import type{EmergencyRecord}from"./types";
const ACTIONS=["Tenter de joindre l’aéronef et confirmer la situation","Aviser le gestionnaire opérationnel selon le plan d’urgence Orizon","Consigner l’heure, la position et les renseignements reçus","Communiquer avec les services ATS/FIC ou les services d’urgence selon le plan approuvé","Préparer les informations sur les personnes à bord, le carburant et l’itinéraire","Maintenir un journal chronologique de toutes les communications"];
export function EmergencyPage(){const[aircraft,setAircraft]=useState<SchedulerResource[]>([]);const[records,setRecords]=useState<EmergencyRecord[]>([]);const[error,setError]=useState("");const[message,setMessage]=useState("");const[form,setForm]=useState({aircraftId:"",openedBy:"",contactEstablished:"Inconnu",nature:"",personsOnBoard:"",fuelEndurance:"",lastKnownPosition:"",lastContactTime:"",intentions:"",transponderCode:"",eltStatus:"Inconnu",weather:"",notes:"",actions:[] as string[]});useEffect(()=>{const requested=new URLSearchParams(window.location.search).get("aircraft");if(requested)setForm(current=>({...current,aircraftId:requested}));},[]);useEffect(()=>{const a=subscribeResources({next:x=>setAircraft(x.filter(r=>r.kind==="aircraft")),error:e=>setError(e.message)});const b=subscribeEmergencies({next:setRecords,error:e=>setError(e.message)});return()=>{a();b();}},[]);const selected=aircraft.find(x=>x.id===form.aircraftId);function toggle(x:string){setForm(c=>({...c,actions:c.actions.includes(x)?c.actions.filter(a=>a!==x):[...c.actions,x]}));}async function submit(e:React.FormEvent){e.preventDefault();if(!selected||!form.openedBy.trim()||!form.nature.trim()){setMessage("L’avion, la personne responsable et la nature de l’urgence sont obligatoires.");return;}await createEmergency({...form,aircraftRegistration:selected.name,reservationId:""});setMessage("Dossier d’urgence créé et journalisé.");setForm({...form,aircraftId:"",nature:"",notes:"",actions:[]});}const open=useMemo(()=>records.filter(r=>r.status!=="Résolue").sort((a,b)=>b.openedAt.localeCompare(a.openedAt)),[records]);return <><PageHeader title="Urgences" subtitle="Questionnaire guidé et journal d’actions"/>{error&&<div className="notice error">{error}</div>}{message&&<div className="notice">{message}</div>}<div className="emergency-warning"><strong>Important</strong><p>Ce module soutient la collecte d’information. Il ne remplace jamais le plan d’intervention d’urgence approuvé d’Orizon Aviation, les procédures ATS ni les services d’urgence.</p></div><div className="emergency-layout"><section className="card"><h2>Ouvrir une urgence</h2><form className="form" onSubmit={submit}><div className="form-grid"><label>Avion<select required value={form.aircraftId} onChange={e=>setForm({...form,aircraftId:e.target.value})}><option value="">Sélectionner</option>{aircraft.map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</select></label><label>Dispatch responsable<input required value={form.openedBy} onChange={e=>setForm({...form,openedBy:e.target.value})}/></label><label>Contact établi?<select value={form.contactEstablished} onChange={e=>setForm({...form,contactEstablished:e.target.value})}><option>Inconnu</option><option>Oui</option><option>Non</option><option>Intermittent</option></select></label><label>Dernier contact<input type="time" value={form.lastContactTime} onChange={e=>setForm({...form,lastContactTime:e.target.value})}/></label><label>Personnes à bord<input value={form.personsOnBoard} onChange={e=>setForm({...form,personsOnBoard:e.target.value})}/></label><label>Autonomie carburant<input value={form.fuelEndurance} onChange={e=>setForm({...form,fuelEndurance:e.target.value})}/></label><label>Dernière position connue<input value={form.lastKnownPosition} onChange={e=>setForm({...form,lastKnownPosition:e.target.value})}/></label><label>Code transpondeur<input value={form.transponderCode} onChange={e=>setForm({...form,transponderCode:e.target.value})}/></label><label>État ELT<select value={form.eltStatus} onChange={e=>setForm({...form,eltStatus:e.target.value})}><option>Inconnu</option><option>Non activée</option><option>Activée</option><option>Signal reçu</option></select></label><label>Météo connue<input value={form.weather} onChange={e=>setForm({...form,weather:e.target.value})}/></label></div><label>Nature de l’urgence<textarea required value={form.nature} onChange={e=>setForm({...form,nature:e.target.value})}/></label><label>Intentions / renseignements reçus<textarea value={form.intentions} onChange={e=>setForm({...form,intentions:e.target.value})}/></label><label>Notes<textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><div><strong>Actions proposées par le plan interne</strong><div className="emergency-actions">{ACTIONS.map(a=><button type="button" className={form.actions.includes(a)?"active":""} onClick={()=>toggle(a)} key={a}>{form.actions.includes(a)?"✓ ":""}{a}</button>)}</div></div><button className="button danger">Ouvrir le dossier d’urgence</button></form></section><section className="card"><h2>Urgences ouvertes</h2>{open.map(r=><article className="emergency-record" key={r.id}><strong>{r.aircraftRegistration} — {r.nature}</strong><span>{r.openedAt.slice(0,16).replace("T"," ")} · {r.openedBy}</span><p>Contact: {r.contactEstablished} · Position: {r.lastKnownPosition||"inconnue"}</p><button className="button secondary" onClick={()=>updateEmergency(r.id,{status:"Résolue"})}>Marquer résolue</button></article>)}{!open.length&&<p>Aucune urgence ouverte.</p>}</section></div></>}
