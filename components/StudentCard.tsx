import Link from "next/link";import {Student,Cohort} from "@/lib/types";
export function StudentCard({student,cohort}:{student:Student;cohort?:Cohort}){const alertClass=student.alertType==="danger"?"danger":student.alertType||"ok";return <Link href={`/students/${student.id}`} className="card student"><div><div className="name">{student.firstName} {student.lastName}</div><div className="muted">Instructeur : {student.instructor}</div></div><div><span className={`badge ${student.pathType}`}>{student.path}</span><div className="muted" style={{marginTop:6}}>{cohort?.name||"Aucune cohorte"}</div></div><div><strong>{student.progress}%</strong><div className="progress"><div style={{width:`${student.progress}%`}}/></div></div><div><div className="muted">Dernier vol : {student.lastFlight||"Aucun"}</div><span className={`badge ${alertClass}`}>{student.alert||"OK"}</span></div></Link>}
