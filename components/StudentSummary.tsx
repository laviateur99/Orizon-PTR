import Link from "next/link";import {Student,Cohort} from "@/lib/types";
export function StudentSummary({student,cohort}:{student:Student;cohort?:Cohort}){return <section className="card panel"><div className="avatar">{student.firstName[0]}{student.lastName[0]}</div><h2>{student.firstName} {student.lastName}</h2><div className="muted">{student.path} · {cohort?.name||"Aucune cohorte"}</div><div className="muted">Instructeur : {student.instructor}</div><div className="section">Progression</div><strong>{student.progress}%</strong><div className="progress"><div style={{width:`${student.progress}%`}}/></div><div className="statgrid"><div className="stat"><span className="muted">Total</span><strong>{student.total} h</strong></div><div className="stat"><span className="muted">Dual</span><strong>{student.dual} h</strong></div><div className="stat"><span className="muted">Solo</span><strong>{student.solo} h</strong></div><div className="stat"><span className="muted">Instrument</span><strong>{student.instrument} h</strong></div></div><div className="actions"><Link className="button" href={`/flights/new?student=${student.id}`}>+ Nouveau vol</Link><Link className="button secondary" href={`/schedule/new?student=${student.id}`}>Réserver</Link><Link className="button secondary" href={`/students/${student.id}/assign`}>Assigner</Link></div></section>}
