import {Reservation,Student,Instructor,Aircraft} from "@/lib/types";
export function ReservationCard({r,student,instructor,aircraft}:{r:Reservation;student?:Student;instructor?:Instructor;aircraft?:Aircraft}){const cls=r.type==="Solo"?"solo":r.type==="Maintenance"?"maintenance":"dual";return <div className={`reservation ${cls}`}><strong>{r.startTime} - {r.endTime} · {r.type}</strong><div className="muted">{student?`${student.firstName} ${student.lastName}`:"Aucun étudiant"} · {aircraft?.registration||""} {aircraft?.type||""}</div><div className="muted">{instructor?`Instructeur : ${instructor.firstName} ${instructor.lastName}`:""}</div><div>{r.lesson}</div><span className="badge blue">{r.status}</span></div>}
